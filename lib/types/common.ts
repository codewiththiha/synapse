/**
 * Common shared types used across the application
 */

/**
 * 2D position coordinate
 * Used for cursor positions, drag positions, and element coordinates
 */
export interface Position2D {
	x: number;
	y: number;
}
